# prometheus-postgres-exporter values
# Chart: prometheus-community/prometheus-postgres-exporter 7.5.2 (app v0.19.1)
# Скрапирует target-db (PostgreSQL 16) в namespace databases

config:
  datasource:
    host: target-db.databases.svc.cluster.local
    port: "5432"
    user: appuser
    password: apppassword
    database: appdb
    sslmode: disable

serviceMonitor:
  enabled: true
  labels:
    release: kube-prometheus-stack   # обязательно — Prometheus отбирает SM по этому лейблу
  interval: 30s

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 64Mi
