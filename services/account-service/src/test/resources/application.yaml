spring:
  config:
    import: "optional:configserver:"
  profiles:
    active: test
  flyway:
    enabled: true
  cloud:
    openfeign:
      circuitbreaker:
        enabled: false

customer-service:
  url: http://localhost:9876

resilience4j:
  retry:
    instances:
      customer-service:
        maxAttempts: 1
