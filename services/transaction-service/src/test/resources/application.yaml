spring:
  config:
    import: "optional:configserver:"
  profiles:
    active: test
  flyway:
    enabled: true
  cloud:
    openfeign:
      circuitbreaker:
        enabled: false

account-service:
  url: http://localhost:9877

resilience4j:
  retry:
    instances:
      account-service:
        maxAttempts: 1
